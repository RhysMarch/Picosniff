#app-grid {
    layout: grid;
    grid-size: 1;
    grid-columns: 1fr 1fr; /* Two columns */
    grid-rows: auto 1fr 1fr; /* Three rows, sizes adjusted by content */
}

#top-left-pane {
    overflow-y: scroll;
    border: round dodgerblue;
    content-align-horizontal: center;
    height: auto;
}

#middle-left-pane {
    width: 100%;
    border: round dodgerblue;
    height: auto;
}

#bottom-left-pane {
    width: 100%;
    height: auto;
    border: round dodgerblue;
}

#right-pane {
    overflow-y: auto;
    dock: right;
    row-span: 3;
    height: 100%;
    width: 60%;
    border: round dodgerblue;
}

#logo {
    content-align-horizontal: center;
}

#commands {
    content-align-horizontal: center;
}

#packet-counts-table {
    content-align-horizontal: left;
    /* top, right, bottom, left */
    padding: 1 1 0 1;
}

#packet-flow-plot {
    content-align-horizontal: center;
    /* top, right, bottom, left */
    padding: 0 1 0 1;
}

#packet-counts-barchart {
    content-align-horizontal: center;
    /* top, right, bottom, left */
    padding: 1 1 0 1;
}

#packet-flow-plot-section, #packet-counts-barchart-section, #packet-counts-table-section {
    height: 50%; /* Make sure each section takes full available space */
    overflow-y: auto; /* Scroll appears only when necessary */
}